server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: crypto-toolbox-backend
  servlet:
    multipart:
      max-file-size: 100MB
      max-request-size: 200MB

logging:
  level:
    com.smtool: INFO
