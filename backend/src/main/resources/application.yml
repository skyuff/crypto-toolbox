server:
  port: 8080
  servlet:
    context-path: /

spring:
  application:
    name: crypto-toolbox-backend
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 100MB

logging:
  level:
    com.smtool: INFO
